let T = Int(readLine()!)!

for _ in 0..<T {
    let info = readLine()!.split(separator: " ")
    let N = Int(info[0])!
    let M = Int(info[1])!
    
    for _ in 0..<M {
        let abInfo = readLine()!.split(separator: " ")
    }
    
    print(N-1) // 무슨... 문제가 이래?
}